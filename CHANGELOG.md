# Patch Notes

Plain-English patch notes for the `temalimit` strategy and the rest of the NT8 trading stack, written the way a game studio ships them: one patch per day, newest on top, with **New / UI / Balance / Fixed / Under the hood / Known issues** sections and *dev notes* where the reasoning matters. This used to live in the `soy` repo's README; `park` is now the single home for changelogs and wireframes/diagrams.

See [wireframes/](wireframes/) for diagrams (referenced inline below). Static reference material lives at the [bottom of this file](#reference-template-mode-3-rotation-logic) so the newest patch always stays on top.

---

## Patch 2026-07-20 — "Dead Man's Switch"

Two ES short trades filled into a fast spike and then ran with **no stop loss at all**. Three separate safety nets were supposed to catch that. All three were broken, in three different ways. This patch fixes all three and adds a fourth that checks every tick.

### 🐛 Fixed
- **The trailing stop was barely working on three of the four markets.** As a trade moves in your favour, the strategy is supposed to drag the stop up behind it, locking in more profit the further it runs. It does that properly on the Nasdaq — the market the chart is actually set to — where it adjusted the stop 164 times in one hour this morning. On the S&P, the Dow and the Russell, which are attached to the same chart as extra feeds, it managed **2 and 11 adjustments over the same hour and a comparable number of trades**. Those three markets have effectively been trading with a stop that gets set once early and then sits there. It now updates on every price tick for every market, not just the chart's own.
  *Dev note: the interesting part is that the strategy was not failing to notice the run-up — it was noticing too late to act. It measures the best price reached by reading the high of the current bar, which is correct, but on an attached feed it only looks when that bar finishes. By then price has usually come back off the high, so the stop level it now wants to set sits above where the market currently is. A safety check added yesterday refuses to place a stop above the market (it was added for good reason — placing one there fires it instantly). So the request gets thrown away entirely and the stop stays exactly where it was. Late measurement plus a correct safety check equals a stop that never moves at all. Trailing on ticks fixes it by setting each new level on the way up, while that level is still below the market and therefore legal.*
  *Worked example — this is the trade that exposed it. A Dow long this morning entered at 52198 and ran 65 points in your favour, a peak of $325. The stop moved once, four minutes in, to lock in $30, and then never moved again for the next ten minutes while all of that run happened. It closed for $90. Had the stop kept pace it would have been sitting at $172 by the peak. The $90 was itself lucky — the stop was sitting at a level worth $30 and happened to fill twelve ticks better on a gap.*
  *Honesty note on the evidence, because it cuts both ways: this could not be properly A/B tested. Replaying the day needs tick-by-tick market data that either isn't downloaded or doesn't cover the window, and a standard backtest can't reproduce the fault at all. Comparing the Nasdaq (where trailing works) against the other three across 190 trades shows **no difference at all** in how much of a winning move gets captured — 42.3% versus 42.5%. Only when you narrow to the big runs, where a trailing stop is actually supposed to earn its keep, does the Nasdaq pull ahead (69% versus 58%). That's a cut chosen after seeing the flat headline number, on 49 trades, comparing different markets — suggestive, not proof. The mechanical fault is beyond doubt; that fixing it makes money is not. Worth watching rather than assuming.*
- **Every stop-out was being recorded twice.** Whenever a trade closed at its stop, the strategy logged it once correctly and then, on the very next bar, logged it *again* as if you had manually closed the position by hand. You hadn't. 42 duplicate trades had piled up since the feature went live yesterday evening, inventing **$3,106.25 of profit that never happened** across nine accounts — and feeding those made-up numbers into both the daily loss cut-out and the template scoreboard that decides which settings to trade next.
  *Dev note: the strategy keeps a backup record of the open trade in case it loses track of it. When a stop fills, that backup is deliberately left in place and cleared a moment later. The problem was where "a moment later" sat — the code that writes the dashboard status ran first and read the backup, saw a trade that the broker had already closed, and concluded someone must have closed it by hand. The duplicate-protection that was supposed to catch exactly this only worked in one direction: it covered "closed by hand, then the fill arrives", never "the fill arrives, then the stale backup is read". Fixed by reconciling the backup before anything reads it, and only when the account is genuinely flat — that last condition is what keeps it from interfering with a trade recovered after a restart, where the backup is the only record that exists.*
- **The first attempt at that fix only covered half the problem.** It corrected the check that runs when a new price bar forms — but the strategy also refreshes its position display every 5 seconds off live price ticks, precisely so that charts which only update on a completed bar don't go stale in between. That second path had the same flaw and hadn't been touched. On a one-minute chart the 5-second refresh gets there first, so the fake trade was still recorded.
  *Dev note: the evidence was in which trades survived. After the first fix, two stop-outs on fast tick-based charts came through completely clean, while one on a one-minute chart still produced a duplicate — its stop filled at 08:05:38 and the next bar wasn't until 08:06:00, leaving a 22-second window the 5-second refresh walked straight into. That split is the useful signal for next time: duplicates appearing ONLY on slower chart types point at a refresh path rather than the bar path. The reconciliation is now a single shared routine called from all three places that read the position, so a future fourth reader is the only way this comes back.*
- **Verified against NinjaTrader's own execution list rather than assumed.** For one account across an eleven-hour window, NinjaTrader recorded 15 exits. All 15 matched the trade log exactly, to the second and to the tick. The 11 extra "closed by hand" entries matched nothing at all — 15 real fills, 26 logged trades.
  *Dev note: worth writing down because the duplicates do not look like duplicates. Only the entry details match; the exit time, exit price, profit and exit reason all differ, so a pair reads as two separate trades at a glance. Worse, the fake row is stamped with the bar's time rather than the moment it was written, so on tick and line-break charts it can appear to happen an hour BEFORE the real exit it duplicates. Sorting by time actively misleads here.*

- **The trend dashboard's new "is it alive" alarm cried wolf on the Nasdaq within hours of being installed.** Last night the alarm was rewritten to watch each robot's heartbeat — a marker written every time it processes a bar — rather than watching for prediction requests, which only happen when a setup forms and so went quiet on a normal thin Sunday. That part was right and stands. What was wrong was the deadline attached to it: a single flat two hours for every market. This morning it declared the Nasdaq silent for 312 minutes. It wasn't broken. NinjaTrader's own log shows the robot running without interruption overnight, and inside that stretch the Nasdaq went **281 minutes between heartbeats while perfectly healthy** — because its bars are built from order-flow imbalance, and a balanced overnight market simply doesn't produce them. Meanwhile the euro was writing one every three to six minutes. No single deadline can serve both: anything patient enough for the Nasdaq is asleep through a euro outage lasting hours. Each market now learns its own normal rhythm and is judged against that — the alarm sounds at three times an instrument's own worst typical gap, never sooner than two hours. Until a market has shown enough of its rhythm to characterise it, it gets a deliberately generous eight-hour benefit of the doubt rather than a guess. The warning text now also states which deadline it used and whether it was learned or provisional, so the next alert can be judged without opening the code.
  *Dev note: the wrong turn is worth recording. The first diagnosis argued the Nasdaq must be broken because its order-flow setting (400) is smaller than the S&P's (500), so it ought to produce bars more often, not less. That comparison is meaningless — those numbers sit on different scales on purpose, which is exactly why each market gets its own, and the code says so in a comment right at the top of the file. Reading it as a like-for-like ratio manufactured a fault out of nothing and sent a real outage investigation down a blind alley. The rule to keep: judge a market against its own history, never against another market's settings. User caught this one.*
  *Second dev note, unrelated but costly: the restart link returns straight away while the old server keeps answering for several more seconds. Checking whether a change took effect immediately after clicking it reads the dying process and shows the old behaviour — which briefly looked like the fix hadn't worked at all. Wait for the new process before believing a post-restart page.*

### 🧹 Housekeeping
- **The 42 fake trades were removed and the template scoreboards rebuilt from scratch.** Two genuine hand-closes from 01:10 were kept — both match real fills in NinjaTrader. Everything is backed up before the change.
  *Dev note: one of those 41 appeared after the fix was written, which looks alarming and isn't. Editing the strategy file doesn't take effect until NinjaTrader rebuilds it, and that rebuild finished at 07:51:16 — 64 seconds after the trade in question closed. It ran on the old code. Worth knowing in general: there's a gap between saving a change and it actually running.*
- **Five trades from this morning's flatten had the wrong exit prices** and were corrected against the real fills. They had been recorded using an estimated price rather than what actually filled, which overstated them by **$740.00** — every single one in the flattering direction, because an estimate reads the last chart bar rather than what the market actually paid.
- **A scoreboard split-brain got fixed along the way.** Some strategy instances were still running older code and writing their results to a second, obsolete set of files, so that profit never reached the scoreboard that modes 4 and 5 actually read. The rebuild pulls it all back in. It also picked up yesterday's two hand-reconstructed trades, which had been added to the trade history but never to the scoreboard.
  *Dev note: the first attempt here was to simply subtract the fake numbers back out. A dry run stopped it — it would have written negative balances for trades the scoreboard had never recorded in the first place, quietly inventing a second problem while fixing the first. Rebuilding everything from the trade history instead makes the files provably consistent with the trades that actually happened: 379 of 379 accounted for.*

### ✨ New
- **Max day margin: a ceiling on how much margin can be tied up at once.** Switch it on in the strategy settings (Risk group), give it a dollar figure, and the strategy refuses any new trade that would push total committed margin past it. With a **$1500** cap and ES, RTY and YM at $500 each, all three can be open together — a fourth trade waits until one closes. With **$1000**, it's either one NQ, or one ES *and* one RTY. Set it to **$500** and NQ can never open at all, since one contract already costs more than the whole allowance. Off by default; nothing changes until you turn it on.
  *Dev note: the margin figures are never typed into the strategy — they're read back out of NinjaTrader at the moment of every single trade attempt, because brokers move day margins around (usually before a holiday or an expiry) and a number hard-coded today is wrong next month. Nothing to update and no restart when that happens. Worth knowing: this counts **everything open on the account**, not just what this strategy did — other strategies, other copies of temalimit, and anything opened by hand all spend the same margin, so a cap that only watched its own trades could be blown through from the side. It also counts orders that are placed but not yet filled. That last part sounds pedantic and isn't: this strategy rests its entries as limit orders, so without it four symbols could each look at an empty account in the same instant, all say "plenty of room," and then all fill.*
  *Dev note 2: if NinjaTrader has no day margin recorded for a symbol, the trade is **blocked**, not allowed through, and the log line tells you exactly where to set it (Instruments → that symbol → Risk). The alternative — treating "don't know" as "costs nothing" — would leave the cap quietly switched off for precisely the symbol nobody configured, which is the one failure a safety limit is not allowed to have. The same applies to anything already open: if some position on the account can't be priced, the running total is an undercount, so it refuses to trade on a number it knows is wrong instead of trading on it anyway. Same rule if the check itself errors — it stops, rather than guessing. The practical consequence to be aware of: one unconfigured symbol sitting open, even one opened by hand, will pause new entries until you either set its margin or switch the cap off. That's the intended trade for a limit you can actually rely on, and the log always names the symbol.*

### 🐛 Fixed
- **A rejected stop no longer leaves a trade running unprotected.** When the strategy's protective stop was refused by the broker, it tried to close the trade immediately — but it was asking a fraction of a second too early, before the trade had officially been recorded, and the request was quietly thrown away. The log shows the refusal and then simply nothing. The close request is now queued and re-issued a tick later, once the trade definitely exists.
  *Dev note: the timing gap is tiny and completely invisible without the log — the refusal arrives at 00:27:13.127 and the trade registers at 00:27:13.129, two thousandths of a second later. The old code looked correct in every reading; it just never ran. This is the one that turned "a stop got refused" into "a trade ran for 20 minutes with nothing protecting it."*
- **The "the market already blew past our stop" check now actually runs.** It was written to compare the intended stop against the current price — but it ran *after* the stop had already been pulled back to a safe distance, so it was comparing a number against itself and could never be true. It had been unreachable in all four places it appears, for both long and short trades. It now compares before adjusting.
- **A refused stop no longer blocks its own replacement.** The strategy remembers the last stop price it sent so it doesn't spam the broker with tiny adjustments. It was recording that price when the order was *sent* rather than when it was *accepted*, so a refused stop still counted as "already handled" — and every later attempt to place that same stop was skipped as a duplicate. It now only records stops the broker actually confirmed.
- **Every temalimit strategy stopped running at 04:31:07 and nothing noticed for 2h43m.** All of them, on the same second, across different accounts and markets. Six positions sat open with nothing managing them. A restart cleared it.
  *Dev note: no root cause. What it definitely wasn't: the daily-loss cut-out (last fired July 9, and no loss-limit exits today) and a frozen platform (NinjaTrader was responsive, prices were still arriving, and a different strategy in the same process kept writing every 30 seconds throughout). That last detail is what made it diagnosable at all — one strategy dead while its neighbour ran normally — and it's now the exact signature the new alarm watches for. Recorded here honestly as unexplained; if it recurs, the alarm catches it in 15 minutes instead of 163.*

### 🔧 Under the hood
- **New tick-by-tick guard: if a trade is open and nothing is protecting it, do something about it.** Every tick, the strategy now checks whether a working stop actually exists. If the intended stop level is still valid, it re-places it. If the market has already run past it, it closes the trade instead of quietly re-arming further out.
  *Dev note: re-arming was the tempting option and it's the wrong one. On the ES trade, "just place a legal stop" would have parked it 14.5 points away on a trade whose intended risk was 9.5 points — turning a refused stop into a silent 50% risk increase nobody asked for. Once the market is past the planned exit, the risk budget is already spent; the honest move is out.*
- Verified before shipping: strategy-state bookkeeping passes its parity check (153 fields, all mirrored), the syntax parse is clean, and the strategy recompiled successfully at 00:48.
- **New alarm: "a trade is open and nothing is protecting it."** A sixth watchdog now checks every 60 seconds and pings your phone if any account has held a position with no protective stop for more than 90 seconds. On the trades that started all this it would have alerted about **40 minutes before** they were spotted.
  *Dev note: the important design choice is where it reads from. The obvious source — the strategy's own open-trades exports — is exactly the source that failed here: those files froze mid-incident still showing a healthy-looking position, so an alarm built on them would have sat silent. This one reads NinjaTrader's own log instead, which gets written whether or not the strategy is alive. That's what makes it work in the case that actually matters: a strategy that has died, been switched off, or lost track of a position it still owns. It also matches on which direction the stop would close (a sell stop protects a long, a buy-to-cover protects a short) rather than trusting the order's name. Tested both ways — silent on today's healthy positions, and on a replay of the incident it flagged both naked trades while correctly clearing a third position on the same account, same second, that did have a stop.*
- **The two trades that went unrecorded have been reconstructed** and added to the trade history, so realized P&L, the template ledger and the ML data are no longer missing them: −$750.00 on template 7 and −$587.50 on template 13.
  *Dev note: every figure was sourced rather than guessed — prices and times from NinjaTrader's log, the data series and template from the frozen export files, and the strategy-setting columns computed from the template table in code and then checked against a live trade on the same template today, which matched exactly. Four columns were deliberately left blank: they're live volatility readings taken at entry on a Renko-1 and a HeikenAshi-1 chart, and nothing comparable exists to derive them from. A blank can be filtered out later; an invented number would look like a real measurement forever.*
- **The alarm caught a third one within two minutes of being switched on.** A short on a different account had been sitting with no stop since 04:09; the alarm flagged it at 04:32 and kept escalating every 15 minutes. Same failure, third account — which settles that this was a recurring hole and not a one-off.
- **Second alarm added: "the strategy has gone quiet."** If temalimit stops writing for 15 minutes while NinjaTrader is still running and still receiving prices, your phone gets a separate alert. This is the one that mattered most today.
  *Dev note: the trigger for adding it is the stall described above — the strategy showed **Enabled** in the Control Center while not actually running for over two and a half hours. "Enabled" and "executing" turn out to be different things, and nothing was checking the second one. The alarm deliberately requires two conditions at once: temalimit silent AND something else demonstrably alive. That pairing is what keeps it quiet on weekends and holidays without needing to know a single thing about market hours — when the market closes, everything goes quiet together, and that isn't a fault. If the check that looks for NinjaTrader can't answer, it assumes it's running, so a broken check fails loudly instead of quietly switching itself off.*

### 🎨 UI
- **The Live dashboard's jump bar now has a button for each chart type.** After the five section buttons there's a divider and then one button per chart type on the board — Line Break, Tick, Range, Volume, Renko, Minute, Heiken Ashi — each jumping straight to that card. The list follows the time range you've picked: only chart types that actually traded inside it get a button, so switching to a single quiet day drops the bar from seven to five, and they're ordered to match the cards down the page. This is not the old "Accounts" button coming back — that one pointed at the top of the board and was removed on request; these point at individual cards.
- **Two things about the highlighted button were quietly wrong, and got fixed with it.** The bar underlines whichever card you're currently looking at. Where two cards sit side by side it was always crediting the right-hand one, which on a normal-width window meant the entire left column of chart-type cards could never be highlighted at all — three of the seven new buttons would have been permanently dark. And the very last card on the page sits further down than the page can actually scroll, so its button could never light up either: scroll to the bottom and you'd see the card filling the screen while the bar still pointed at something above it. Both fixed — ties now go to the left-hand card, and reaching the bottom of the page counts as arriving at whatever is down there.
  *Dev note: the second one is the more interesting failure. The bar decides where you are by measuring against a line 130 pixels down the screen, which is fine everywhere except the end of the page, where scrolling stops before that line ever reaches the final card — measured at 4108 pixels down against a scroll that stops at 3803. Neither bug was new; both were invisible while the bar only listed five stacked sections, and both became unmissable the moment it listed seven cards laid out in a grid. Worth noting as the general shape: adding the feature didn't break these, it revealed them.*
  *Dev note 2: one visible side effect, deliberately accepted. Pending / Live / Completed Trades also sit side by side on a wide screen, so that row now highlights Pending where it used to highlight Completed. Making the tie rule apply to some rows and not others would have been the worse trade.*

### 🧭 Known issues
- **The two trades that triggered all this were not rescued by the fix** and had to be flattened by hand. Both were ES shorts — one entered at 7499.50 with a refused stop at 7507.25, the other at 7502.25 with a refused stop at 7511.75, together about **−$1,340** realized. The fix protects the next trade, but a position that gets orphaned from its strategy across a restart is outside what the strategy can reach on its own — which is precisely the gap the new alarm covers.
- **Why the 04:31 stall happened is still unknown.** It cleared on a full restart and everything has run normally since, so there's nothing left to examine live. The alarm now bounds how long a repeat could go unnoticed, but it doesn't explain it.
- **A near-miss worth remembering rather than a bug.** After a "flatten everything," the alarm reported six positions still open and unprotected, which looked like a false alarm caused by it not understanding the flatten command. It wasn't. The connection dropped seconds after the flatten was issued and before any of it filled, and the disconnect had already cancelled every protective stop — so those positions really were open and unprotected for about 105 seconds until the connection came back and the closes went through. The "obvious fix" would have taught the alarm to go quiet in exactly that situation. It was not made, deliberately: the alarm trusts only confirmed position changes, never an instruction that might not complete.
- The underlying trigger — the strategy's price reading briefly lagging the broker's during a fast spike — is mitigated here, not eliminated. The new guard catches the consequence; it doesn't make the two readings agree.

---

## Patch 2026-07-19 — "Regime Change"

The market's July 16–17 selloff set off two data alarms. This patch teaches the alarms the difference between "the market changed" and "the data broke," fixes a stat freeze in background tabs, and adds a hardware monitor for the machine itself.

### ✨ New
- **The time-range filter from the Live dashboard now exists on all four pages.** Models, Ops, and Trend each get the same 1D/2D/3D/5D/All picker Live has, counting days the way the trading desk does — a "day" starts at 3pm California, not midnight. On Models and Trend it filters the sample- and prediction-derived panels (counts, label mixes, recent activity); the model-health tables intentionally ignore it, since "is training healthy" is a now question, not a last-Tuesday question — each picker's tooltip spells out which panels it covers. The Ops page turned out to already *have* the picker — it moved over from the Live dashboard with its buttons intact — but the data plumbing behind it was hardwired to "all history," so clicking 1D lit up the button and changed nothing. On top of that, the whole filter bar was hidden as part of the "hide the splash-page sections" carryover from the move, so for a while the connected picker existed but couldn't be seen. Both fixed: the range buttons (and custom range) are visible again — just those; the splash bar's stat and sort widgets stay hidden — and Ops opens on an honest 1D view. Confirmed working on a phone-size screen.
  *Dev note: one subtlety worth knowing when numbers look "wrong": 3D and 1D can legitimately show the same count. Friday's samples logged at 1pm belong to the trading day that started Thursday 3pm — so a 3-day window anchored at Friday 3pm excludes them. The math is right; the anchor is just stricter than a calendar. On Trend, the staleness and anomaly alarms deliberately keep reading unfiltered data — whether the system is alive must not depend on which view you picked.*
- **Laptop hardware monitor.** A standalone background tool watches the machine's memory, pagefile pressure, CPU, and GPU every 15 seconds to answer one question: can this workload live on 64GB of RAM instead of 128GB? It only pings the phone on *lasting* changes — never brief spikes — and every alert ends with a plain verdict: "64GB likely safe," "64GB borderline," or "keep 128GB." Runs on its own notification channel, starts at login, restarts itself on failure. Completely independent of the trading system.
- **The data-integrity checks now also run themselves once a day**, right after the 14:00 model retrain — previously they only ran when someone clicked a button, so a real problem could go unnoticed indefinitely between manual checks. This is the one moment each day the training data actually changes, so it's the only moment a fresh check can find something new.
  *Dev note: prompted by a fair question — "if the Run button is always clickable, how would I know when to actually click it?" The clickable state was never meant to mean "due for a run"; now there's an actual daily run to rely on, and the panel's description spells out the difference.*
- **The "Data Mix" section now shows a live-vs-shadow split under every row** (Labels, Symbols, Data Series, Triggers), matching the breakdown already shown per-market on the Model Health tables. All four sections' splits check out exactly against the total sample count (394 live / 9,375 shadow / 9,769 total).
  *Dev note: user noticed this split existed on the per-market tables but not here and asked for parity. One caveat carried over from the existing per-market tables: clicking the "Count" column header to sort no longer sorts cleanly by the big number when a row has this extra live/shadow line underneath it — a pre-existing quirk this reuses rather than introduces, flagged for a future fix.*

### 🎨 UI
- **Every tab of the Breakdown card now charts its profit and loss, not just one.** Only the Data Series tab had any visual at all — the other four (Instrument, Direction, Session, Exit Signal) were plain number tables, so tabbing across felt like changing pages rather than changing views. Each row's Realized figure now carries a thin bar underneath it, anchored at zero: profit grows right in green, loss grows left in red, scaled so the biggest number in that table is the longest bar. You can see which instrument or exit reason is carrying the account, and which is bleeding, without reading a single figure. The same bar was added to the Data Series list beside the donut, so all five tabs now speak one visual language.
  *Dev note: the obvious move — copy the donut onto the other four tabs — would have been the wrong one, and it's worth saying why. A donut shows each slice's share of a total, which means it can only ever chart something that's always positive; realized P&L is signed. That's exactly why the existing donut charts trade **count** rather than money, and it's why, on that tab, the four losing chart types looked no different from the six winning ones. A zero-anchored bar is the only common mark that carries size and direction at once, which is what makes it work identically on a 2-row tab and a 10-row one. Two details that mattered: each table scales to its own largest value rather than a shared one (otherwise the Exit Signal tab, where one reason accounts for 321 of 343 trades, would flatten every other tab against it), and the ML "No Trade" rows — 124,846 of them, which aren't trades and have no P&L — are excluded from setting the scale and correctly draw no bar at all. The bars also carry no text, which keeps the Realized column sorting as numbers; the Models page's Count column lost numeric sorting exactly this way when a second line of text was added under the figure, so that trap was designed around rather than repeated. Verified on the live page: negatives red-left and positives green-right across all five tabs, sorting correct ascending and descending, and the Data Series list still fits its card without scrolling.*
- **The Live dashboard's jump-to-section bar lost its Accounts and Strategies buttons.** The bar now ends at Breakdown. Both boards are still on the page and unchanged — only the shortcut buttons are gone, at request.

### ⚖️ Balance (data alarms)
- The **label-drift alarm** now needs 40+ recent samples before it may go red; thinner evidence shows an amber note instead.
  *Dev note: two markets were "failing" on 13–17 recent rows. A 30-point swing on a dozen samples is noise, not a labeling break.*
- The **input-drift alarm** now files gauges that measure price level and volatility *by design* under an amber "regime drift" note; red is reserved for inputs that should stay stable in any market.
  *Dev note: once those gauges stopped hogging the report, it turned out the trend also shifted several genuinely stable inputs past the red line — so the alarm stays honestly red for now. Every check that would indicate actual data corruption passes.*
- Both alarm changes mirrored to the trend robot's dashboard (its oil-market label warning dropped from red to amber on the spot).
- The engineers' Ops page now polls the trade server every 5 seconds instead of 2 — the evidence tables only change on the minutes scale.

### 🐛 Fixed
- **The trend robot's dashboard said "Idle — market likely closed" while all six of its markets were wide awake.** The banner at the top of that page is supposed to answer one question: is the system alive? It was answering a different one. It watched for the robot *asking the prediction service for an opinion* — and the robot only asks when a setup actually forms or nearly forms. On a thin Sunday evening that can easily be an hour of silence with everything running perfectly. Worse, oil is currently the only market that has ever produced such a request, so the entire "is anything alive" verdict rested on that one market happening to find a setup within twenty minutes. It hadn't for thirty-eight, so the page declared the market closed. The banner now watches the heartbeat each robot writes on every single bar it processes, whether or not anything interesting happened — a direct signal of "I'm running and my data is flowing." All six markets immediately showed up alive and the banner flipped to Healthy. Two supporting adjustments came with it: the "this one market has gone quiet" alarm moved to the same heartbeat signal, and its patience doubled from one hour to two, because how often a bar closes depends entirely on how busy the market is — at the moment of the fix the euro had ticked seven minutes ago while the Nasdaq's last bar was sixty-four minutes old, and both were perfectly healthy. The old one-hour limit would have cried wolf over the Nasdaq. Retired accounts also leave heartbeat files lying around forever, so anything untouched for a day is now ignored rather than mourned indefinitely.
  *Dev note: this is a cousin of the freshness-gate bugs above, but the lesson is its own — the alarm was measuring the wrong thing rather than measuring the right thing badly, and no threshold tuning would have saved it. Worth noting the near-miss during the fix: had the liveness signal been switched to heartbeats while leaving the per-market quiet alarm on the old signal, a harmless "Idle" would have become a permanent false warning the moment oil went quiet — a strictly worse outcome than the bug being fixed. When a banner has two alarms reading the same source, they move together or not at all.*
- **On the Live dashboard, picking a longer time range made chart-type cards disappear rather than appear.** Ask for the last 7 days and the Volume, Range and Heiken Ashi cards simply weren't there — even though those chart types had 27, 11 and 4 trades inside the week you asked for. The cards were being built correctly and then hidden on the way out the door. The page has a separate, deliberate rule that retires a card once its chart type hasn't traded in 24 hours, so the board doesn't accumulate ghosts of setups no longer in use. That rule was being applied on top of *every* range, including ranges explicitly asking to look further back — so any chart type quiet for a day vanished from a 7-day view that existed precisely to show it. Weekends made it worst: by Sunday almost everything is more than a day stale, so the longer ranges emptied out exactly when you'd reach for them. Now, if you name a range, the range decides what you see, full stop; the 24-hour retirement rule applies only to the "All" view, where nothing else would ever remove a stale card. Checked all three ways: 7-day shows all seven chart types, 1-day shows the four that actually traded, and "All" still correctly retires the three quiet ones.
  *Dev note: this is the same mistake as the data-series breakdown fix from earlier — a freshness gate and a date filter answering two different questions with one flag. Worth remembering the shape of it: "has this traded recently" is an absolute question about now, "does this have trades in the window" is a relative one about the range you picked, and stacking the first on top of the second silently subtracts from what you asked for. If a third place ever gates on the 24-hour window, check it against a weekend before trusting it.*
- **Two stray keystrokes quietly froze the entire robot codebase — every fix made all evening was written down but never actually running.** A comment somewhere in the chart-data helper began with a single backslash instead of the two slashes that start a comment, and the identical slip sat in the main strategy file. NinjaTrader treats one bad character anywhere as a reason to refuse the whole batch, so it kept running the last version that *did* compile. Nothing looked wrong — the robots kept placing orders normally all night — but they were trading yesterday's logic. Every repair from this evening (the rotation-mode memory fix, hand-closed-trade logging, the trailing-stop hardening, the stop-fill correction) sat on disk doing nothing. Both characters fixed and every file swept for the same slip.
  *Dev note: worth internalizing how quiet this failure is — the strategies keep trading, the log files say nothing, and the only clue in the entire system is a red row in the editor window that nobody has open. Treat "I edited the strategy" and "the strategy changed" as two separate claims from now on.*
- **Killing the prediction service didn't kill the work it had started, so ten of sixteen processor cores stayed busy on nothing.** Model training runs in separate helper processes. The scripts that stop and restart the service identified what to shut down by looking for its startup command — which those helpers don't share — so they were skipped, kept running with no parent, and nothing was ever going to clean them up. Two of them had been chewing through ~10 cores since the evening's earlier restarts. The freshly restarted service was left fighting for scraps: a basic health check went from a fifteenth of a second to nearly five seconds, which is what made the manual retrain button appear frozen (it shows nothing at all until the retrain fully finishes, so "very slow" and "hung" look identical). Both scripts now follow the family tree down and shut the helpers down too.
  *Dev note: with the strays cleared, a manual retrain finished in about three minutes and trained nine of ten eligible markets. Checked before and after: still zero markets cleared the quality bar to gate entries, so the robots continue trading on their plain signal exactly as before.*
- **A model now has to prove it beats a coin flip — and prove it on enough evidence to count — before it's allowed to recommend anything.** Until tonight, a market was promoted from "just watching" to "the model may now offer opinions" on two conditions: a saved model file exists, and at least one trade has completed. Nothing anywhere asked whether the model was any *good*. The two exit models trained tonight scored dead-even with a coin toss and sailed through that check — they'd have started recommending exits the instant the exit system was switched on. Two gates now stand in the way. First, an accuracy floor: score better than 0.55 or stay in watch-only mode. Second, a minimum-evidence floor: the model must have seen at least 100 examples of the rarer of the two outcomes. A model whose record can't be read at all — missing or corrupted results file — is treated as failing rather than given the benefit of the doubt, on the principle that an unverifiable model is not a trustworthy one. Both of tonight's models correctly dropped back to watch-only the moment the change went live, and the log now names every reason a market was held back rather than just the first one it hit.
  *Dev note: the second gate exists because the first one can't stand alone. Tonight's models scored 0.50 — but that was luck cutting in our favour. A model that has seen three examples of "exit" can just as easily score 0.83 by coincidence, and a lone accuracy floor would wave it straight through. The evidence bar was first set at 25 on a rule of thumb, then corrected to 100 by an actual experiment run the same night: holding everything else fixed and varying only how many rare-case examples the model sees, the score climbs 0.44 → 0.47 → 0.52 → 0.53 → 0.56 as that count goes 2 → 5 → 20 → 50 → 100. Fifty examples still measured a coin flip, so 25 would have let noise through. The same experiment carries a sharper lesson: at two examples the score wobbles by ±0.27 between random reruns, meaning the accuracy floor is barely measuring anything down there — the evidence floor is the gate actually doing the work, and the accuracy bar is the second line of defence. 100 also matches the bar the training step itself now uses, so the two stages finally agree. Both checks read from the results file on disk rather than from memory, because the in-memory copy still holds the previous run's numbers at the moment the decision is made — an ordering trap that would have silently scored the wrong model. Verified with 21 checks, including the specific case the accuracy floor alone would miss.*
- **The first two exit-timing models ever trained finished successfully — and are worth exactly nothing, which is useful to know.** Both long-running training jobs completed and saved their models before being shut down. But look at what they learned from: the ES model saw 70,218 examples of "keep holding" and precisely **one** example of "exit now"; the NQ model saw 37,215 versus **three**. A student shown one counter-example cannot learn the difference, and the scores confirm it — both models perform at exactly coin-flip accuracy. They're stamped "trained OK" all the same.
  *Dev note: the trap worth knowing about is that "has a saved model file" is currently enough to promote a market to recommendation mode — there's no check that the model is better than a coin flip. That's harmless right now because the exit system is globally switched off and only collecting data, and the gate for letting a model actually close real trades is properly strict (it needs 150 completed trades, a genuinely better-than-chance score, and four weeks of varied data — these have 9 and 21 trades, coin-flip scores, and two weeks). But an accuracy floor belongs in that promotion check before the exit system is ever switched on.*
- **Winners-First on NQ picked template 1 — the one thing it should never do.** The new Losers-First/Winners-First rotation modes are supposed to open on the biggest loser or biggest winner in the profit ledger, but a fresh NQ instance in Winners-First mode calmly started on template 1, which wasn't even in its ledger. Three separate problems, found by pulling that one thread:
  1. **The ranking ran before the strategy knew which account it was on.** The "start at the top of the ranking" step runs during startup, early enough that the account name can still be unknown — and the ledger's filename contains the account name. Wrong name → file not found → empty ranking → keep the default template, silently. The strategy now re-checks the ranking a second time at the moment it goes live (when the account is guaranteed known), and only then locks in its opening pick — with a printed line saying what it changed and why.
  2. **The profit ledger was filed by the wrong things — blending chart types, while splitting accounts.** It was kept per account + market, so a 1000-volume NQ instance and a Renko NQ instance on the same account shared one pot of "how has template 38 done?" — even though the same template behaves completely differently on different chart types. Meanwhile every account kept its *own* pot, so a freshly-created account started with a blank record and nothing to rank, even when an identically-configured account had months of history on the exact same market and chart type. Both are now the other way round: the ledger is filed **per market + chart type, with all accounts pooled** — how a template performs on NQ 1000-volume is a fact about the template and the chart, not about whose account placed the order. Mode 3's separate "already tried" checklist is filed exactly the same way, for the same reason — whether template 12 has been tried on NQ 1000-volume is a fact about the chart, not about the account, so two accounts on the same market and chart no longer each re-test the full set of 40 from scratch. All 27 ledgers were rebuilt from the master trade log, which always recorded the chart type. Dropping the account from the key also brought back 207 older trades that had to be discarded earlier in the day — they predate the trade log recording accounts at all — so the rankings now count **343 of 343 trades instead of 136**. Old files archived, not deleted. For the curious: on NQ 1000-volume the all-time board is T39 +$6,670, T40 +$2,210, T26 +$1,275, T38 +$1,075, T35 +$50 — thin samples, but that's what Winners-First will now actually walk.
  3. **Trades closed by hand never made it into the ledger at all.** The ledger only heard about exits the strategy itself ordered. Manual closes and crash-recovery bookings — which yesterday's fixes taught the *trade log* to record — skipped the ledger entirely, so a template's ranking could drift from its true record (one NQ template was short $367.50 from a single evening). All three paths now write the ledger, with the same photo-finish handling the trade log already has: if a "manual close" turns out to have been the strategy's own stop, the ledger is corrected to the true fill price rather than counted twice.
  *Dev note: the T1 sighting that kicked all this off actually had two different causes in sequence — the startup-timing bug above, and then, after that was fixed, a brand-new account with a genuinely empty ledger, which is the designed fallback rather than a bug. The second one is what made the account-pooling decision obvious. Pooling the mode 3 checklist was initially argued against on the grounds that instances would starve each other of untried templates; a 400-run simulation of the actual rotation code showed that's simply not how it fails — when the checklist empties, every instance falls back to plain round-robin and keeps trading. What the simulation did find, with two accounts on one market and chart: pooled covers all 40 templates in 58 trades versus 89, and cuts duplicated testing from ~38% to ~24%, at the cost of the two instances more often holding the same template at the same moment (about 31% of trades versus 10%) — because "lowest untried" is deterministic, so siblings reading one list land on the same answer. That last part is a position-correlation trade-off worth knowing about, not a data problem. Verified the whole file still parses clean after every one of today's edits (zero syntax errors via a standalone compiler pass), since a single typo anywhere under bin/Custom silently freezes all strategies on stale code.*
- **The pullback-state file writer now waits out virus scanners instead of giving up.** The strategy exports a small dashboard file after every bar close, and occasionally logged "unable to write after 5 attempts — file in use by another process." Investigation showed the writer itself is fine; the trouble is that the safe atomic-swap technique it uses makes Windows treat every update as a brand-new file, which sends Defender and the search indexer to re-scan it — and their brief lock sometimes outlasted the writer's old tenth-of-a-second patience. The writer now retries with growing pauses for up to about 1.3 seconds. Missing a write was always harmless (the next bar rewrites everything), so this just quiets the noise and keeps the dashboard fresher.
- **A trade closed by the strategy's own stop-loss got recorded as if a human had closed it — now the record fixes itself.** Yesterday's "manual flatten" safety net logs any trade the account closes out from under the strategy, using the current market price since the true fill isn't known yet. Tonight it caught its first false positive: an NQ trailing stop filled at 20:47:20, but the "account is flat" news reached the strategy a split second before the news of its own stop filling did, so the trade went into the books as a manual close at an estimated price — off by less than a point, and filed under the wrong reason. The safety net still fires exactly as before, but now, if the strategy's own exit fill shows up moments later, it goes back and corrects the record: right reason ("Stop loss"), right price, right profit number, and the daily P&L total is adjusted by the difference. Trades a human genuinely closes are untouched — there's no strategy fill coming, so nothing rewrites them.
- **The chart-data helper no longer prints its routine "late callback, discarding" housekeeping note to the NinjaTrader output window.** It was working as designed — a slow data fetch answered after a faster retry already won — but it read like an error and cluttered the log. The behavior is unchanged; only the noise is gone.
- **The exit-model's first real training run asked the machine for 311 gigabytes of memory — and kept asking, every few minutes, all evening.** In a way this was a success story gone wrong: yesterday's exit-model repair worked, ES 3LineBreak trades finally closed on Sunday's session, and that market's exit model became eligible to train for the very first time. But its training data is unusual — nearly 70,000 bar-by-bar snapshots spread across just nine trades, some of which sat open for tens of thousands of bars — and the training code packed up, for *every single bar*, a complete copy of everything that came before it in that trade. The bill came to 311GB in one allocation. Windows spent minutes at a time gamely swelling its swap file toward that number before giving up, and during those minutes the whole prediction service went unresponsive — that's what the "ML POST FAILED" timeouts in NinjaTrader were, along with a watchdog-forced restart at 19:08. Failing didn't stop it: 200 samples later (about three minutes at Sunday-evening pace) it tried again, forever. Training examples are now capped at each trade's last 128 bars — which is all the model ever actually read; its architecture makes its call from the end of the sequence — bringing the worst case from 311GB down to about a third of one. Live predictions trim to the same window so the model sees in production exactly what it saw in school.
  *Dev note: the three sibling 3LineBreak markets (RTY, NQ, YM) were queued up to hit the identical wall as soon as their trades started closing — this fix lands before any of them got there. Separately: the afternoon's scattered timeout bursts around 2:43–4:37pm were nothing of the sort — each one lines up exactly with someone clicking the dashboard's own Restart button, and the hidden duplicate SYSTEM-level watchdog task found during the same investigation (it was double-checking the service every minute and defeating the "fail twice before restarting" safety) has been deleted by hand.*
- **On the Models page, the floating navigation pills no longer sit on top of a card's title.** Clicking a pill to jump to a section could land you with the top of that card hidden underneath the pill bar — especially on narrower windows, where the header wraps onto two lines and gets taller than the fixed amount of headroom the page reserved for it. The page now measures the header's actual height (and re-measures whenever the window is resized) and scrolls sections just past it. The pill bar also got a more opaque, blurred background so card content no longer shows through it.
- **Template rotation mode 3 no longer forgets its place after a restart.** Re-enabling the strategies after tonight's NinjaTrader crash sent every "Unused Only" instance back to template 1, even though each one had faithfully saved the template it was on. That reset was actually written into the code on purpose — "on startup, always jump to the lowest template that hasn't traded yet" — but a template only earns its "has traded" mark when a trade *closes*, and none of these instances had closed one yet, so "lowest untried" always meant template 1. Mode 3 now picks up exactly where its save file says it left off, the same way the other rotation modes always have; the jump-to-lowest-untried still happens where it belongs, after each completed trade.
  *Dev note: the crash was a red herring — nothing was lost, the save files were intact and correct. A clean restart on any day would have done the same thing. The Losers-First/Winners-First modes (4/5) still deliberately re-rank from the profit ledger on startup: their "saved place" is the ranking itself, not a template number.*
- **Trades you close by hand now show up on the Live dashboard instead of vanishing.** Until today, the trade log only heard about exits the strategy itself ordered — stop losses, targets, timed exits. Close a position from NinjaTrader's own buttons (or have the platform force-close one), and the trade evaporated: no row in the trade file, nothing on the dashboard, as if it never happened. Two real trades disappeared this way tonight after a NinjaTrader crash forced a manual cleanup. The strategy now covers both ways this happens: if it's running and notices the account went flat underneath it, it writes the trade down on the spot (marked "ManualFlatten", priced at the market's level that second); and when an instance starts up and finds a leftover "open trade" note from a session that died mid-position, it books that orphan as a manual close too — so tonight's stranded SimRenko trade will file itself the moment that instance is re-enabled.
  *Dev note: the racy case — a stop fills at the exact moment we decide the account "went flat manually" — is deduplicated by a per-market flag, so a trade can be labeled slightly wrong in a photo finish but can never appear twice. One casualty is unrecoverable: the Simpointandfigure trade closed at 7:11pm had its open-trade note wiped before this fix existed, so its ~+$53 lives only in NinjaTrader's own execution history.*
- **SimRenko got kicked off a second time the same evening — from the other side — so the whole kill mechanism is now gone.** Two hours after the morning fix (next bullet), the re-enabled instance went long NQ and its trailing stop chased the rising price tick by tick, each move parking the stop at the bare legal minimum distance below the market. A 1.5-point dip arrived in the split second one of those moves was in transit, the platform refused it, and — same as before — dumped the position and shut the robot off. This time the strategy was already measuring from the correct side of the book, which proved the deeper problem: any refusal, however harmless, was a death sentence. Two changes: the trail no longer chases — if the level it wants is too close to the market, it simply keeps the stop it already has and tries again on a later tick; and a refused order no longer shuts the strategy down. Instead the strategy handles the two ways a stop can fail on its own: a refused *adjustment* keeps the old stop working (which it verifiably still is) and carries on, while a refused *initial* stop — the one case where the position is truly unprotected — flattens immediately. **The SimRenko instance needs re-enabling one more time.**
  *Dev note: this closes the "residual exposure" flagged in the morning entry. The platform's all-or-nothing error setting was the real single point of failure — five-plus occurrences of this error family since mid-July, and every one ended in a disabled instance regardless of what caused it. The morning trade this killed was +11 points; this evening's was +3.5. The errors were never protecting anyone.*
- **The SimRenko robot got kicked off the field mid-trade — by a fix from two days ago.** Ten minutes after Sunday's open it was short NQ and winning, tried to tighten its safety stop, and the platform refused the move ("stop price can't be below the market"), then did what it always does on a refused order: dumped the position and switched the robot off. The trade still banked +11 points, but the instance sat disabled after that. The cause was Friday's repair to this exact family of errors — it told the strategy to measure "where the market is" from the *selling* side of the order book when a short position's stop must legally clear the *buying* side. On a normal weekday the two sides sit close enough that nobody notices; at Sunday's reopen, when the book is thin and the two sides drift far apart, the stop landed in the gap between them and got refused. All three places that make this check now measure from the correct side (buy stops check the ask, sell stops check the bid — now written down in the code so it can't be "fixed" backwards again). **The disabled SimRenko instance needs to be re-enabled in NinjaTrader.**
  *Dev note: Friday's diagnosis described an impossible mechanism (a stop clamped above the ask cannot be "below the market") — the real culprit then was rapid-fire stop updates racing the platform, which the 300ms throttle added the same day genuinely fixed. The side-swap rode along and lay dormant until the first wide-spread moment. A stop rejected this fast (one attempt, 70 seconds after entry) is what ruled the throttle out and pointed at the spread.*
- **Switching time ranges on the Models page took 9–31 seconds.** Every switch re-read the entire training-sample archive from disk — a 131MB file, because each of its ~10,000 rows carries a full snapshot of the market bars around it — just to recount a few labels and percentages that never look at those snapshots. The page now keeps a slimmed-down copy in memory (the bar snapshots dropped — nothing on the dashboard ever displayed them) and, crucially, when live trading appends a new sample it reads *only the new bytes at the end of the file* instead of starting over. Range switches went from 9–31 seconds to about a quarter of a second, and stay fast even while trading is actively adding samples.
  *Dev note: the naive cache wasn't enough — the first version re-parsed all 131MB every time a single sample was appended, which during market hours is constantly. The append fast-path is guarded by two fingerprints (start of file, end of the already-read region) so a genuine rewrite of the file — retrain dedupe, data purge — is detected and re-read in full rather than trusted. Verified with a real append landing mid-test: no slow spike. The 5-second full parse still happens once after each retrain rewrite; that's the honest floor.*
- **Both live strategy instances crashed and shut themselves off at Sunday's market open — twice.** At exactly 15:00:00, on the very first price tick of the trading week, both instances died; re-enabled, they died again about half an hour later. The culprit: in multi-market mode, each market keeps its own running VWAP (average-price) tape, but every one of those tapes was accidentally pinned to the *chart's main market* for its sense of "which bar is current." The two markets' bar counts normally stay close enough that nobody notices — but this chart opened Sunday with no history at all, so the moment a secondary market ticked before the main one, a tape was asked to write to a bar that didn't exist yet. The crash-on-re-enable was the same mismatch from the other side: the strategy's first eligible trade check walks ~33 bars of history down a tape whose "current bar" belonged to a different market. Each tape is now pinned to its own market.
  *Dev note: the quieter half of this bug matters more than the crash — whenever the two markets' bar counts drifted apart, secondary markets were feeding slightly time-shifted VWAP values into the ML feature windows. Wrong silently, on normal weekdays, with no error. Also added: if the strategy ever crashes mid-bar again, the Output window now prints the exact method and line that failed instead of the platform's generic "index out of range" message, which names nothing and cost hours today. The platform still shuts the strategy off — that part is correct for live trading — it just stops being anonymous about why.*
- **The strategy could finish starting up half-built.** A single line in its startup log — `'barsAgo' needed to be between 0 and -1 but was 0` — turned out to be much more than a bad log message. The strategy was asking "what time is it?" the way it does mid-session, by reading the clock off the current price bar. During startup there is no current bar yet, so the question threw an error, and that error quietly cancelled *the rest of startup*: the indicator sets it trades from, and the entire template rotation state, were never built. It now falls back to the wall clock before any bars exist.
  Two smaller faults the same bug exposed were fixed alongside it: the "compiled successfully" banner marked itself as shown *before* actually showing it (so once it failed, that startup message was gone for good rather than retried), and both startup messages could permanently redirect every later log line into the wrong Output tab if they failed mid-write — which is probably why the follow-on errors never appeared where you'd look for them.
  *Dev note: the error was being caught and printed by a safety net wrapper, which is exactly why it looked cosmetic — one tidy line in the output and no crash. But the wrapper sits around the whole startup routine, and the throw happened near the top of it, so everything below was skipped. The one-word tell was in the message itself: "between 0 and -1" is the platform saying it has minus-one bars. Also fixed the debug-print helper, which had the same flaw and would have thrown while trying to report the original error. Every other place the strategy reads the bar clock was checked — they all run mid-session, where it's valid.*
- **The Active Templates card listed templates from configs that aren't running anymore, and from days ago.** Each strategy instance leaves a small "which template am I on" note on disk — but the note's filename includes the chart type, so switching an instance to a new chart type left the old note behind forever, and the card kept showing it as if it were live. Simvolume, for example, showed its four current Volume-chart templates *and* three leftover rows from a Range-chart setup retired on July 9. The card now keeps only the freshest note per account + market — 52 rows instead of 61, every one of them the instance's *current* template. On top of that, the card now only shows instances that have rotated **today** — where "today" starts at 3pm California time, matching this system's trading day — so an instance sitting untouched since a prior session drops off the list instead of lingering with a stale template. Wireframe: [active_templates_current_only_wireframe.svg](wireframes/active_templates_current_only_wireframe.svg).
  *Dev note: dedupe by newest file timestamp per account + instrument, then a same-trading-day cutoff on top. The two rules are independent: dedupe picks each instance's one true current row, the day cutoff decides whether that row is recent enough to bother showing. Confirmed the cutoff behaves right at the boundary (currently 16:05 PT, cutoff correctly lands at today's 15:00 PT) — before /restart the card had 52 rows across two trading days, after it dropped to 16, all from the current one.*
- **Fixed-template instances (mode 0) were completely invisible on the Active Templates card** — and the new "rotated today" rule above would have kept them that way forever, since a fixed template never rotates. The deeper surprise: mode 0 had *never* appeared on that card, because the strategy only writes its "which template am I on" note during rotation events, and mode 0 has none. The strategy now writes that note once at startup for fixed-template instances too, and the card exempts them from the "rotated today" rule — a template that's pinned on purpose isn't stale, it's just pinned.
  *Dev note: the note file gained a real second field (it was a dead always-zero slot) carrying the template mode, encoded negatively so the thousands of old files with a literal zero there can't be confused for fixed-mode instances — misreading them would have resurrected every stale row the earlier fix just hid, permanently, since dead files never get rewritten. Verified end-to-end with planted files: a 3-day-old new-format fixed-mode file shows, a 3-day-old legacy file stays hidden. One expectation to note: a mode-0 instance already running keeps the old code until its next restart, so it appears on the card after that, not immediately.*
- **A rotating instance that's busy trading could also silently fall off the Active Templates card.** Follow-up question from the same review: what about a rotating mode where only one template qualifies, so it never actually changes? That case turned out to be fine — the rotation timer still fires and re-stamps the note file even when "next" is the same template. But chasing it exposed the real gap: the timer only runs while the instance is *flat with no working order*. Sit in a position for the afternoon, or leave an entry order working, and the instance writes nothing for hours — then vanishes from the card at the 3pm day boundary while it's arguably at its most alive. The strategy now re-stamps its note file every 30 minutes no matter what it's doing, so a running instance can never age off the card.
  *Dev note: heartbeat runs before the rotation-eligibility check on purpose, since that check is exactly what was starving the saves. The timestamp updates even if the disk write fails, so a persistent write error paces itself at one retry per 30 minutes instead of one per bar. Running instances pick this up on their next restart.*
- **The Models dashboard kept jumping to a different card on phones.** Try to read the Active Templates card on mobile and within a couple of seconds you'd be looking at something else. Six cards on that page each refresh themselves every 15 seconds, and each refresh tore down and rebuilt its card even when nothing had changed — and on a phone, every rebuilt table also flashed into its wide desktop layout for a split second before restacking. Any of that happening *above* the card you're reading shoves the page around; with six timers firing on their own schedules, it happened every few seconds. Cards now only redraw when their data actually changed, and when they do redraw on a phone they come back already stacked — no flash, no shove. Bonus: the little "view last run" result panels under the Ablation and Verification tables no longer get wiped every 15 seconds.
  *Dev note: the browser normally anchors your scroll position to the content you're reading, but rebuilding that content from scratch destroys the anchor. The wide-layout flash was a 300ms debounce on the phone-layout stamper; redraws now restamp immediately instead of waiting for it.*
- Animated counters (the big profit number, stat cards) froze at stale values while a dashboard tab sat in the background. They now update instantly whenever the tab isn't visible and only animate while you're actually watching.
- **The live dashboard's "Data Series" breakdown could show 0 trades no matter what time range you picked** — including "All time" — for an actively-traded strategy that simply hadn't traded in the last 24 hours (like over a weekend). Every other breakdown tab (Instrument, Direction, Session, Exit Signal) kept showing that same strategy's trades just fine, so only this one tab looked broken.
  *Dev note: an old "hide deprecated strategies" filter was being applied to Data Series before the time-range filter ever ran, so no range selection could undo it. Removed that filter from this one spot — it now follows the same rules as its sibling tabs. Confirmed fixed across All/5D/3D/2D/1D, matching the Instrument tab's counts exactly at every range.*

### 🔧 Under the hood
- **The exit robot's retraining had quietly turned into a permanent background job pinning the machine.** Two of the fastest-logging chart types (ES and NQ 3-Line-Break) were retraining essentially non-stop — each retrain took over an hour, but a new one came due every ~4 minutes, so the moment one finished the next started. The rule was "retrain every 200 new observations," which is sensible for a small group but ignores that the work itself grows with the group's *entire* history: at 70,000 observations, those 200 new rows were triggering an hour of work. Retraining is now spaced out in proportion to how much history a group has. Small groups are untouched and still retrain every 200 observations; the big 3-Line-Break groups go from every ~4 minutes to roughly hourly. Retraining also no longer grabs every CPU core on the box, so it can't crowd out the live trading decisions it shares the machine with.
  *Dev note: this is the same pair of groups behind last night's 311GB memory blowup — that fix stopped the crash but left the underlying churn, which only became visible as sustained CPU load once training could actually complete. Worth stating plainly: these groups have logged tens of thousands of observations from just 9 and 20 finished trades, and none of their models are being used for live decisions yet, so slowing this down costs nothing real today. The restart baseline is seeded from each group's existing count, so restarting the service no longer sets off a simultaneous retrain of every large group.*

- **The exit robot was burning hours of CPU training on data it could not possibly learn from.** Follow-up to the retraining-cadence item above, which turned out to be treating a symptom. Spacing the retrains further apart helped and was working exactly as built — but the machine went straight back to full load, and the reason was in the finished models themselves: they had learned *nothing*. One chart type had **2 "exit" examples against 90,627 "hold" examples**; another had 5 against 39,665; two more had zero exits at all. A model trained on that scores 0.5000 — a coin flip, the exact number you'd get by guessing. The permission check only asked "has this group seen at least one of each outcome?", and a single row answered yes. It now requires real evidence: at least 50 examples of the rarer outcome, *and* at least 1% of the group. All six expensive chart types stop training immediately and resume on their own if they ever accumulate enough genuine variety. Nothing of value is lost — none of these models fed a live trading decision.
  *Dev note: two lessons worth writing down. First, an honest correction — the earlier estimate of how fast these groups log data was measured before the Sunday open and was 6x too low; the real rate is ~351 rows/minute. Second, and more useful: spacing retrains proportionally to group size **cannot** fix this kind of overload, no matter how the dial is set. Training cost grows at exactly the same rate as the gap between retrains, so the group size cancels out and the dial only picks a fixed level of oversubscription — at the current setting, retraining takes 10.6x longer than the gap it's given; even the most aggressive sane setting still lands at 1.1x. The dial stays in place as cheap insurance, but the real fix was refusing to start.*

- **Three weeks of exit-training data was missing the one thing that mattered — and nothing was checking.** Asked whether any data was actually poisoned, the answer turned out to be yes, though not in the obvious way. Every trade entered before July 18 — **613 of 644** — never recorded the moment it closed. Each one is a long transcript of "still holding, still holding, still holding" that simply stops, with the exit silently absent. That's the fingerprint of the bug fixed on July 18, and this data is the proof it worked: zero trades logged an exit before that date, and 20 of 28 have since. Those rows aren't false — the trade genuinely was being held during those bars — but with the ending cut off they teach the robot that positions never close. All 613 trades (185,032 rows) are now moved into an archive folder, not deleted, and verified row-for-row: nothing lost, and **not one genuine "exit" record was moved** — all 20 stayed put. A new **Exit-label integrity** check now runs alongside the other data alarms and fails if any *closed* trade is missing its exit, with trades still running excluded so an open position never trips it.
  *Dev note: worth being clear that the archiving fixes nothing by itself, which is why it wasn't rushed. Modelling the purge first showed that not a single chart type becomes trainable afterward — no group anywhere has more than 5 exit records total, against a floor of 50. The value is preventing bad rows from re-entering later and keeping the evidence. Also checked and cleared: no scrambled columns, no data logged under the wrong market, no duplicated rows, and no double-counting (the ~5 rows/second is genuine bar formation, not a logging loop).*
- **The new check found a live problem within a minute of being switched on.** Five trades from July 19 — *after* the fix — closed without recording their exit, including one with 18,175 rows. All five had gone quiet for 3–4½ hours while their chart types kept logging, so they're definitely closed rather than still open. So the July 18 fix is roughly 80% effective, not complete.
  *Dev note: the cause is genuinely not established yet and shouldn't be guessed at. One lead: three of the five went quiet within a couple of minutes of each other, shortly after a service restart, which would fit their exit records being lost while the service was unavailable. Against that theory, NinjaTrader's own logs record no failed sends at all that day, and the other two don't line up with any restart. Next step is checking whether the exit record is sent fire-and-forget with no retry — but that's a look, not a conclusion.*

- **A closing trade now refuses to lose its exit record.** The cause of the missing exits from the item above turned out to be the delivery, not the logging. When a trade closes, the strategy sends one final "here's how it ended" message — and it was sent the same throwaway way as the thousands of routine "still holding" updates: fire once, and if it fails, shrug. A trade sends about 2,000 of the routine ones and exactly **one** of the important one. Miss that single send — because the service was restarting, or just busy past a 2.5-second cutoff — and that trade's ending is gone for good. It now retries four times, and if the service still isn't answering, writes the message to a file on disk and keeps re-sending it every minute until it lands. The routine updates deliberately keep the old cheap behaviour; they're redundant by design.
  *Dev note: an honest retraction from the earlier entry — I'd said NinjaTrader's logs showed no failed sends, treating that as evidence against this theory. That check was wrong twice over: those messages are rate-limited to one per 30 seconds, and they go to the Output window rather than the log files I searched. Also worth recording a trap for next time: the syntax checker was first run with its output pointed at a null path, which makes the compiler quit before reading a single line — producing a confident "no errors" that meant nothing. The real check found the file clean, and NinjaTrader rebuilt it the same minute.*
- **Studied how much data the exit robot actually needs — and the study contradicted the plan.** The assumption all evening was that the lopsidedness of the data (roughly 2,400 "holding" rows per "exit" row) was what stopped the robot learning. Tested directly, that's **false**: holding everything else equal and varying the lopsidedness across the entire range, the model scored identically every time. What actually decides it is the plain number of *finished trades* — below about 50 the score is indistinguishable from a coin toss, and at 2–5 it swings so wildly that a "good" score is pure luck. That precisely explains the 0.5000 seen earlier: it wasn't a measurement, it was noise.
  *Dev note: this changes the recommendation and corrects two of my own settings from earlier tonight. Trimming the data is still worth doing — capping each trade at its final 128 updates removes 96.5% of the rows and turns a two-hour retrain into minutes — but the justification is **cost, not accuracy**. Meanwhile the 1%-minimum rule I added is the wrong shape and should be relaxed, since it would block genuinely usable data that merely sits in a large pile; and the 50-example minimum is, if anything, too generous. The real bottleneck is simply that these chart types have completed 1–6 trades each, and no amount of clever sampling invents more.*

### 🧭 Known issues
- Two drift alarms remain honestly red (NQ 60-Range labels; several trend-geometry inputs): the market genuinely looks different after July 16–17. Expected to clear as new data accumulates — still red in a week means it's a real signal worth acting on.
- The 3-Line-Break and Renko chart types log tens of thousands of "keep holding" rows per finished trade and almost no "exit" rows, because a single trade can span that many bars. Until that shape changes, those groups will keep skipping exit-model training by design. Worth revisiting how exit samples are recorded for these bar types rather than raising the floors.
- Five July-19 trades closed without recording an exit (see above). Until that's understood, the exit-label check will keep failing — correctly. It should not be silenced to make the dashboard green.

---

## Patch 2026-07-18 — "Three Dashboards, One Product"

The big one: a full dashboard redesign around a single rule — **the live page is for anyone; the other pages are for engineers** — plus a data-honesty audit that caught two serious problems, and the discovery that the exit-AI had never learned anything, ever.

### ✨ New
- **Ops page** (port 8765/ops): all six auto-tuning evidence panels that used to crowd the live page (Reassess Activity, Template Coverage & Usage, No-Fill Log, ATR Pullback, Sizing Reassess, Entry Gate Reassess) now live in one engineers' room, fed live from the same data.
- **Verification Suite panel** on both AI servers: a row of on-demand integrity checks ("does the AI collapse to random guessing when we scramble the answers on purpose?", "are practice trades leaking into the exam pile?"), each with Pass/Warn/Fail and saved results. This suite is what caught both data-honesty bugs below.
- **Reassess Activity card**: day-by-day view of exactly what the automatic settings-editor found and decided, with All / Applied / Rejected / Skipped / Dry-run filters.
- **Active-profile table**: a live dashboard list of every running strategy copy (account, contract, chart type) and which settings profile it's on right now — no more opening individual chart logs.
- **Two rotation modes**: *Losers First* trades only currently-losing profiles starting with the biggest loser (a losing profile deserves another look before a winner is touched); *Winners First* is the mirror image. Both re-rank continuously off real profit/loss.

### 🎨 UI
- **Live page (8766) is now the splash page**: one big profit/loss number with the equity chart and time-range buttons under it, the trade lists, and a tabbed "Breakdown" card (market / direction / session / exit reason). A "Models" header chip answers "are the robots healthy?" with a green dot.
- **Models page (8765) leads with the verdict**: a checks-passing chip plus a red "Attention" bar that appears only when something is actually failing. Five distribution tables merged into one tabbed "Data Mix" card.
- **Trend page (8767) went card-first**: each market gets a status card with progress toward its 100-sample training gate; the giant 14-column table is one click away. Four "Recent…" tables merged into one tabbed "Activity" feed.
- **One visual language everywhere**: flat dark panels, no neon or glow, red/green reserved strictly for loss/win and fail/pass, no emoji. Every page links to the other three; restart/retrain moved into a "…" menu so they can't be fat-fingered.
- **Phones work now**: tables fold into label/value rows on small screens (Models and Trend previously had no phone layout at all), and the live page gets a thumb-reach bottom tab bar. Card dragging hides behind an explicit "Edit layout" button.
  *Dev note: nothing about trading logic, data collection, or the AI changed in the redesign — purely presentation.*

### ⚖️ Balance
- **Entry-price cushion restored** on the loosest dozen profiles per contract. A chain of small automatic adjustments had walked it down to essentially "fill immediately, no cushion" — those trades filled far more often and lost money on 3 of 4 contracts, while wider-cushion trades made money on all 4. Restored to intended sizes, with a safeguard so stale evidence can't walk it back down.

### 🐛 Fixed
- **~420 practice trades were filed under the wrong market.** The strategy juggles four markets in one program, and a bookkeeping gap let a trade opened while watching one market get recorded under another. All wrong-market records were purged (backups kept), affected models retrained, and two tripwires added — each practice trade now refuses to be recorded under any market but its own, and a "Cross-instrument bleed" check scans the whole pile on demand.
- **The AI's report card was too easy to ace.** A model that lazily always says "don't trade" scored near-perfect on quiet stretches — one such model had earned veto power and silently blocked 12,000+ potential S&P entries in three days. Grading now requires beating the "always say don't-trade" score by a real margin on a test with enough genuine buy/sell moments. Today zero models hold veto power; every market trades on plain technical signals until a model honestly earns it.
- **The exit-AI had never learned anything — ever.** It logged 185,000+ "still holding" examples and exactly zero "exit" examples, because the code meant to fire when a trade closes checked a flag that could never come out true. Fixed; exit examples accumulate from the next closed trade onward.
- **The exit-AI's readiness check graded a nonexistent placeholder** (its daily "has it earned more control?" query was missing the chart-series field), so the answer was always "not ready." Fixed.
- **An AI-server outage no longer halts trading.** Previously, if the strategy couldn't reach its AI helper it gave up on the entry entirely; now an outage falls back to plain technical signals — the same safe fallback used when the AI is online but unconfident.
- **Two more "propose the same change and reject it forever" bugs** in the automatic settings-editor: a mathematically-unreachable risk suggestion now moves partway as far as it safely can, and "already at the ceiling" is now a calm state instead of a stuck suggestion. Its skip log (bloated to ~2,190 lines of repeats) now logs each distinct finding once per day.
- **Practice-trade bookkeeping cleanup**: pretend trades now use the exact same entry-distance math as real trades (they'd drifted onto an easier-to-fill formula), plus four smaller measurement fixes (overwritten snapshots, inconsistent timestamps, undercounted wait times, near-misses measured against pre-order prices).
  *Dev note: none dramatic alone, but all five fed the automatic settings-editor — cleaning them up makes its suggestions trustworthy.*
- **Session-boundary snap**: crossing the overnight/day boundary now immediately switches to an allowed profile instead of trading a disallowed one for up to a full no-fill window (10–23 minutes).

---

## Patch 2026-07-17 — "The Great Bug Hunt"

A full sweep of the trend robot, the practice-trade pipeline, and the machine's safety nets — plus the settings-editor going autonomous (with guardrails) and a two-tier risk formula.

### ✨ New
- **The dashboard now edits the strategy's own settings automatically** — every 5 minutes, and only when every profile with enough real evidence for a given contract-and-setting agrees on the same direction. Guardrails: measured evidence required for tightening directions, the full 40-profile curve is re-simulated before any write (rejected if any invariant would break), a full file backup before every edit, and every decision logged.
  *Dev note: watching it run live caught two real bugs its design review missed — a suggestion built on a single real measurement, and a rounding loop that "corrected" the same value every 5 minutes forever.*
- **No-Fill Log suggestions**: every unfilled order now records how close price actually came; the dashboard suggests a smaller cushion where orders consistently miss by similar amounts, or a bigger one where every order fills with room to spare. Suggestions need 5+ real measurements.
- **Sizing Reassess card**: per contract and profile, how far real trades moved against the position before turning, and how much peak profit was captured vs. given back. Reversal trades show plain dollars ("gave back $860 of $845 peak") because percentages off small peaks mislead.
- **Trend dashboard health banner**: flags a market that goes quiet while its neighbors stay live (the closest it can get to noticing a switched-off strategy), and actively watches for the exact data mix-up in the crude-oil bug below — two markets showing identical buying-pressure numbers, or one frozen.

### ⚖️ Balance
- **Two-tier risk sizing.** Profiles 1–19 keep the smooth growth curve; profiles 20–40 grow on their own curve so profile 40's risk is settable independently. The S&P specifically steps a fixed $12.50 per profile above 19 — the only way its coarse tick size never lands two profiles on the same dollar.

  | Contract | Profile 1 | Profile 40 |
  | --- | --- | --- |
  | Nasdaq (NQ) | $500 / $375 | $1,000 / $800 |
  | S&P 500 (ES) | $312.50 / $300 | $812.50 / $787.50 |
  | Russell 2000 (RTY) | $200 / $150 | $500 / $375 |
  | Dow (YM) | $100 / $75 | $400 / $300 |

  *(per-trade risk / daily "keep trying" budget — verified across all 160 combinations: always increasing, never duplicated, NQ > ES > RTY > YM at every profile.)*

  ![Two-tier risk sizing wireframe](wireframes/risk_sizing_wireframe.svg)

### 🐛 Fixed
- **Five real bugs in the trend robot**, headlined by: *all nine markets were feeding the AI crude oil's order-flow data* (a hardcoded market #0), so the gold, Nasdaq, and bitcoin models were learning from crude's numbers while believing them their own. 713 of 779 contaminated practice trades purged (crude's own 66 kept, backups first) and two already-trained markets reset to "still learning." Also fixed: the every-5-bars "do you still like this trade?" exit never ran (math overflow made "5 bars yet?" permanently false), four end-of-day close-out settings did nothing, one-market retrains secretly retrained everything, and the daily 2:05 PM self-retrain could serve half-updated answers mid-rebuild.
- **A file-sync accident had silently deleted recent work** — including the entire "AI picks the next profile" feature — while NinjaTrader kept running the older compiled copy. Caught because the code was missing from disk yet still writing fresh training data. Restored and rebuilt on top of the new risk formula.
- **A 22,000-point "price swing" in the practice data** (impossible for a ~700-point-range contract) led to a sanity clamp: no single-trade excursion beyond a generous limit gets recorded; a warning is logged instead so a repeat is visible rather than silently poisoning the data.
- **Same-day dashboard glitch**: a text-quoting style broke two model-health sections after a restart. Fixed, and dashboard changes now get their browser-side code actually executed in a real check before shipping.

### 🔧 Under the hood
- **Per-market memory checker**: a standing tool now verifies all ~150 pieces of per-market state survive the swap when one strategy instance juggles multiple markets — it immediately found (and fixed) two unsaved rotation fields.
- **Practice and real trades now share one filter core.** The three indicator pass/fail rules existed as hand-duplicated copies — risky, since practice trades teach the AI. Consolidated to shared cores with thin wrappers, verified identical across 200,000 randomized cases.
- **Safety nets hardened**: the daily Mega backup now includes the strategies' full version-control history (not just file snapshots); a new phone alert fires below 10GB free disk; and a local-only background task snapshots the two live strategy files every 5 minutes for instant rollback (never uploaded anywhere — strategy files don't push to GitHub).

---

## Patch 2026-07-16 — "The Formula"

### ⚖️ Balance
- **Risk-per-trade is now a formula, not a 160-number hand-typed price list.** Dollar risk still grows smoothly with profile number, and no two profiles can ever risk the identical amount (the old list had accidental duplicates). Unknown contracts fall back to the ES row, as before.

  | Contract | Per-trade risk (Profile 1) | Daily budget |
  | --- | --- | --- |
  | Nasdaq (NQ) | $500 | $375 |
  | S&P 500 (ES) | $400 | $300 |
  | Russell 2000 (RTY) | $200 | $150 |
  | Dow (YM) | $100 | $75 |

  ![Risk sizing wireframe](wireframes/risk_sizing_wireframe.svg)

---

## Patch 2026-07-15 — "Quality of Life"

### ✨ New
- **AI profile picker** (off by default): the strategy can ask its AI "which profile next?" instead of cycling in order. If the AI isn't confident, normal rotation continues — safe to enable even mid-training. Switch off = identical behavior to before.
- **Phone alerts on order placed and filled** — live account only; demo and test accounts stay silent.

### ⚖️ Balance
- **Entry pullback distances fine-tuned** per contract, growing on looser profiles so the strategy doesn't chase price when settings are already loose.

### 🐛 Fixed
- **Custom Range mode cancelled every fresh order forever** near the overnight/day session switch, because a session restriction that was never meant to apply to it did. It no longer does.

---

## Patch 2026-07-14 — "Unstuck"

### 🐛 Fixed
- **"Unused Only" rotation could freeze on one profile forever**: an unfilled order's timeout kept re-picking "the lowest profile I haven't traded" — but profiles only count as traded once a trade *closes*, so it picked itself endlessly. Timeouts now simply advance to the next profile; "unused first" logic waits for an actual closed trade.
- **Another cancel-forever loop at the session switch**: cancelling a now-disallowed profile's order never updated which profile the strategy thought it was on, so it retried the same doomed setup every candle (over half of 279 logged no-fill cancels landed within a minute of placement). Cancelling now also resets to an allowed profile. Bonus finds: the "ignore session restrictions" toggle wasn't respected in random mode, and the "profiles already used today" file had never actually been saved to disk.

---

## Reference: Template Mode 3 rotation logic

*(Static reference material, not a patch — kept at the bottom so the newest patch stays on top.)*

                ┌──────────────────────────────┐
                │      ACTIVE TEMPLATE: T       │
                │          T = 1...40           │
                └──────────────┬───────────────┘
                               │
                               ▼
            Is this template marked as a winner?
                    │                      │
                  YES                      NO
                    │                      │
                    ▼                      ▼
           69-minute fill window    34m30s fill window
                    │                      │
                    └──────────┬───────────┘
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                    │
          ▼                    ▼                    ▼
      Entry fills          No fill by            Position is
      before deadline      deadline              still open
          │                    │                    │
          │                    │                    └─ Keep template;
          │                    │                       never rotate during
          │                    │                       an open position
          │                    ▼
          │             Advance one:
          │             T1 → T2 → ... → T40 → T1
          │             Arm normal 34m30s window
          │
          ▼
    Trade eventually closes
          │
    ┌─────┴──────┐
    │            │
    ▼            ▼
  WIN          LOSS
    │            │
    ▼            ▼
Stay on T     T2–T39 → T-1
Arm 69m       T1 → T1
window        T40 → T1
              Arm normal 34m30s window

`temalimit` doesn't use one fixed set of rules all day. It has 40 different settings profiles ("templates"), numbered 1 (strictest, fewest trades) to 40 (loosest, most trades), and it rotates through them over time. The picture below shows the rotation logic:

![Template Mode 3 rotation wireframe](wireframes/mode3_wireframe.svg)

In plain terms:
- Every time an order is placed, there's a countdown. If the profile that placed the order has won a trade before, it gets more time to fill (69 minutes); if not, it gets less (about 34 minutes).
- If the order fills in time, the strategy trades it out normally.
- If it doesn't fill in time, the strategy gives up on that profile and moves to the next one in line.
- Once a trade is open, the strategy never switches profiles until that trade is closed.
- After a **win**, it keeps using the same profile. After a **loss**, it steps back one profile (gets a little stricter), unless it was already on the strictest one, in which case it resets to profile 1.
